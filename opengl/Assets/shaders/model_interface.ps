#version 330 core
out vec4 FragColor;

in VS_OUT {
	vec2 texCoord;
} ps_in;

uniform sampler2D texture_diffuse1;

void main()
{
	FragColor = texture(texture_diffuse1, ps_in.texCoord);
	//FragColor = vec4(1.0f, 0.0f, 0.0f, 1.0f);
}
